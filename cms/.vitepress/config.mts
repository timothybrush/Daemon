import { defineConfig } from "vitepress";

export default defineConfig({
  title: "Daemon",
  description: "A live, machine-readable broadcast of what you're doing, building, and thinking",
  cleanUrls: true,
  appearance: false,
  themeConfig: {},
});
